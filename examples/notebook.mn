(class (Point <X Y> (var (x : X)) (var (y : Y))
  (def (move (dx : X) (dy : Y))
    (begin 
      (set! x (+ x dx))
      (set! y (+ y dy))))
  (impl ToString
    (def (toString)
      (f"Point {X} {Y} (x: {x}, y: {y})")))))