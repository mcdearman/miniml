class Point(x : Int = 0, y : Int = 0) = 
  move(dx, dy): () = begin
    x = x + dx
    y = y + dy
  end

  impl ToString =
    toString(): String = f"Point(x: {x}, y: {y})"
  end
end

class Node T =
  var inner : T
  var span : Int
end

enum List T = Nil
  | Pair(head: T, tail: List T)
end