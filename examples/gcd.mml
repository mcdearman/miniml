fn gcd a b =
  if b = 0 then a 
  else gcd b (a % b)

fn gcd a b =
  fn loop a b =
    if b = 0 then a 
    else loop b (a % b)
  in loop a b

fn gcd(mut a: i64, mut b: i64) -> i64 {
    while b != 0 {
        let t = b;
        b = a % b;
        a = t;
    }
    return a;
}
